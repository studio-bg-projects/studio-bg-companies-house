# Run

```shell
sudo docker compose run --rm -w /app node sh -c "npm install"
```

```shell
sudo docker compose run --rm -w /app/ node sh -c "npm run get-guru-all-companies"
sudo docker compose run --rm -w /app/ node sh -c "npm run fill-companies-from-xml"
```
